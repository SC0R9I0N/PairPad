name: CD

on:
  push:
    branches: [ main, feature/*, fix/* ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    needs: []  # optionally: ["test-backend"] if you want CI to block CD

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Add your deployment steps here:
      # - Build Docker image
      # - Push to registry
      # - Deploy to server
      # - Run migrations
      # - Restart service
